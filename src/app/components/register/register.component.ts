import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  standalone: true,
  imports: [],
  templateUrl: './register.component.html',
  styleUrls: ['./register.component.css'] // Cambié `styleUrl` a `styleUrls` para que acepte un array de estilos
})
export class RegisterComponent {
  // Aquí puedes agregar cualquier lógica necesaria para tu componente
}